{
  "name": "luke.dev",
  "description": "luke.dev",
  "version": "4.0.0",
  "license": "UNLICENSED",
  "private": "true",
  "main": "index.js",
  "scripts": {
    "start": "yarn clean && npx @11ty/eleventy --serve --port 4321",
    "build": "yarn clean && npx @11ty/eleventy",
    "preview": "yarn serve _site -p 4322",
    "clean": "rimraf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "rimraf": "^5.0.1",
    "serve": "^14.2.0"
  }
}

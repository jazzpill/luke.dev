{
  "name": "luke.dev",
  "description": "luke.dev",
  "version": "4.0.0",
  "license": "UNLICENSED",
  "private": "true",
  "main": "index.js",
  "scripts": {
    "start": "yarn clean && npx @11ty/eleventy --serve --port 6767",
    "build": "yarn clean && npx @11ty/eleventy",
    "preview": "yarn serve _site -p 6768",
    "clean": "rimraf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "rimraf": "^4.4.0",
    "serve": "^14.2.0"
  }
}
